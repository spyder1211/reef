# Table++ v0.2.0「開発者向け機能強化」実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Web エンジニアが日常業務（スキーマ確認・クエリ調査・本番接続）で Table++ を使えるようにする 8 機能を実装し、v0.2.0 としてリリースする。

**Architecture:** 既存の 3 層構造（main: ConnectionManager + IPC ハンドラ / preload: contextBridge / renderer: Zustand 単一ストア + React）をそのまま踏襲する。新機能はすべて「main にメソッド追加 → IPC ハンドラ登録 → preload に bridge 追加 → ストアにアクション追加 → UI コンポーネント」の既存パターンで実装する。IPC は例外を投げず `ApiResult<T>` 判別共用体で返す（`src/shared/types.ts:28`）。

**Tech Stack:** Electron 31 / React 18 / TypeScript / Zustand / mysql2 / CodeMirror (@uiw/react-codemirror + @codemirror/lang-sql) / Vitest。Phase 8 でのみ `ssh2` を新規追加する。

**実装担当への注意（Opus 向け）:**
- 各 Phase は独立した feature ブランチ + PR で main へ squash マージする（このリポジトリの慣例）。
- コミットメッセージは既存慣例に従い `feat:` / `fix:` / `refactor:` + 日本語要約。
- 検証コマンドは常に `npm run typecheck` と `npm test`。UI 確認は `npm run dev`。テスト用 MySQL は `docker compose up -d`（リポジトリの compose 設定）で起動する。
- 本計画のコードは現行 main（a0078e7 時点）のコードを読んだ上で書いているが、renderer 側は行番号やローカル変数名がずれている可能性がある。**コードブロックは「意図の正」として扱い、貼り付け前に対象ファイルを必ず読んで整合させること。**

**v0.2.0 スコープ外（v0.3.0 以降へ送り）:** PostgreSQL 対応 / ER 図・FK ジャンプ / キーボードショートカットのカスタマイズ / 自動再接続。

---

## Phase 一覧（実装順）

| Phase | 機能 | ブランチ名 | 依存 |
|---|---|---|---|
| 1 | テーブル構造（スキーマ）表示タブ | `feature/schema-view` | なし |
| 2 | SQL エディタのテーブル名・カラム名補完 | `feature/sql-completion` | なし |
| 3 | クエリ履歴 | `feature/query-history` | なし |
| 4 | EXPLAIN 実行 | `feature/explain` | なし |
| 5 | SQL タブ結果の CSV エクスポート | `feature/sql-result-csv` | なし |
| 6 | セルインスペクタ強化（JSON 整形表示） | `feature/json-inspector` | なし |
| 7 | ダークモード（OS 追従） | `feature/dark-mode` | なし |
| 8 | SSH トンネル接続 | `feature/ssh-tunnel` | なし |
| 9 | バージョン 0.2.0 リリース作業 | `docs/release-0.2.0` | 1〜8 完了後 |

各 Phase は独立しており、どの順でも実装可能。ただし Phase 7（ダークモード）は CSS 全域を触るためコンフリクトを避けたい場合は終盤推奨、Phase 8（SSH）は最も大きいので単独で集中して行うこと。

---

# Phase 1: テーブル構造（スキーマ）表示タブ

テーブルタブに「データ / 構造」の表示切り替えを追加する。構造ビューにはカラム一覧（型・NULL 可否・デフォルト値・キー・extra・コメント）、インデックス一覧、`SHOW CREATE TABLE` の DDL を表示する。

### Task 1-1: 共有型 `TableSchema` の追加

**Files:**
- Modify: `src/shared/types.ts`（末尾に追記）

- [ ] **Step 1: 型を追加**

```ts
// テーブル構造表示（Structure ビュー）用のスキーマ情報
export interface SchemaColumn {
  name: string
  type: string // SHOW FULL COLUMNS の Type（例: "varchar(255)", "int unsigned"）
  nullable: boolean
  key: string // 'PRI' | 'UNI' | 'MUL' | ''
  default: string | null
  extra: string // auto_increment / on update CURRENT_TIMESTAMP 等
  comment: string
}

export interface SchemaIndex {
  name: string
  columns: string[] // Seq_in_index 順
  unique: boolean
}

export interface TableSchema {
  columns: SchemaColumn[]
  indexes: SchemaIndex[]
  ddl: string // SHOW CREATE TABLE の結果
}
```

- [ ] **Step 2: typecheck**

Run: `npm run typecheck` → PASS

- [ ] **Step 3: Commit**

```bash
git add src/shared/types.ts
git commit -m "feat: テーブル構造表示用の TableSchema 型を追加"
```

### Task 1-2: `ConnectionManager.tableSchema()` の実装（TDD）

**Files:**
- Modify: `src/main/connection/ConnectionManager.ts`
- Test: `src/main/connection/ConnectionManager.schema.test.ts`（新規）

既存の `primaryKey()` / `autoIncrementColumns()`（`ConnectionManager.ts:66-86`）と同じパターン。識別子のクォートも同じ方式（`` '`' + table.replace(/`/g, '``') + '`' ``）を使う。

- [ ] **Step 1: 失敗するテストを書く**

既存の ConnectionManager 統合テストを開き、テスト用 MySQL への接続方法（host/port/user/database）をそこから流用する。新規ファイルに:

```ts
import { describe, it, expect, beforeAll, afterAll } from 'vitest'
import { ConnectionManager } from './ConnectionManager'

// 既存の ConnectionManager 統合テストと同じ接続設定を使うこと
describe('ConnectionManager.tableSchema', () => {
  const manager = new ConnectionManager()

  beforeAll(async () => {
    await manager.connect({ host: '127.0.0.1', port: 3306, user: 'root', password: 'root', database: 'test' })
    await manager.query('DROP TABLE IF EXISTS schema_test')
    await manager.query(`
      CREATE TABLE schema_test (
        id INT UNSIGNED NOT NULL AUTO_INCREMENT,
        email VARCHAR(255) NOT NULL COMMENT 'メールアドレス',
        age INT NULL DEFAULT 20,
        PRIMARY KEY (id),
        UNIQUE KEY uq_email (email),
        KEY idx_age_email (age, email)
      )
    `)
  })

  afterAll(async () => {
    await manager.query('DROP TABLE IF EXISTS schema_test')
    await manager.disconnect()
  })

  it('カラム情報を返す', async () => {
    const schema = await manager.tableSchema('schema_test')
    const id = schema.columns.find((c) => c.name === 'id')
    expect(id).toMatchObject({ nullable: false, key: 'PRI', extra: expect.stringContaining('auto_increment') })
    const email = schema.columns.find((c) => c.name === 'email')
    expect(email).toMatchObject({ type: 'varchar(255)', comment: 'メールアドレス' })
    const age = schema.columns.find((c) => c.name === 'age')
    expect(age).toMatchObject({ nullable: true, default: '20' })
  })

  it('インデックスを Seq_in_index 順のカラム配列で返す', async () => {
    const schema = await manager.tableSchema('schema_test')
    const composite = schema.indexes.find((i) => i.name === 'idx_age_email')
    expect(composite).toMatchObject({ unique: false, columns: ['age', 'email'] })
    const uq = schema.indexes.find((i) => i.name === 'uq_email')
    expect(uq?.unique).toBe(true)
  })

  it('DDL に CREATE TABLE 文を返す', async () => {
    const schema = await manager.tableSchema('schema_test')
    expect(schema.ddl).toContain('CREATE TABLE')
    expect(schema.ddl).toContain('schema_test')
  })
})
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `npm test -- ConnectionManager.schema`
Expected: FAIL（`tableSchema is not a function`）

- [ ] **Step 3: 実装**

`ConnectionManager.ts` の `autoIncrementColumns()` の直後に追加:

```ts
  // テーブル構造（カラム・インデックス・DDL）をまとめて取得する（Structure ビュー用）。
  async tableSchema(table: string): Promise<TableSchema> {
    if (!this.pool) throw new Error('Not connected')
    const quoted = '`' + table.replace(/`/g, '``') + '`'

    const [colRows] = await this.pool.query(`SHOW FULL COLUMNS FROM ${quoted}`)
    const columns = (Array.isArray(colRows) ? (colRows as Record<string, unknown>[]) : []).map((r) => ({
      name: String(r.Field),
      type: String(r.Type),
      nullable: String(r.Null).toUpperCase() === 'YES',
      key: String(r.Key ?? ''),
      default: r.Default === null || r.Default === undefined ? null : String(r.Default),
      extra: String(r.Extra ?? ''),
      comment: String(r.Comment ?? '')
    }))

    const [idxRows] = await this.pool.query(`SHOW INDEX FROM ${quoted}`)
    const idxList = Array.isArray(idxRows) ? (idxRows as Record<string, unknown>[]) : []
    const byName = new Map<string, { unique: boolean; cols: { seq: number; col: string }[] }>()
    for (const r of idxList) {
      const name = String(r.Key_name)
      const entry = byName.get(name) ?? { unique: Number(r.Non_unique) === 0, cols: [] }
      entry.cols.push({ seq: Number(r.Seq_in_index), col: String(r.Column_name) })
      byName.set(name, entry)
    }
    const indexes = [...byName.entries()].map(([name, e]) => ({
      name,
      unique: e.unique,
      columns: e.cols.sort((a, b) => a.seq - b.seq).map((c) => c.col)
    }))

    const [ddlRows] = await this.pool.query(`SHOW CREATE TABLE ${quoted}`)
    const ddlRow = (Array.isArray(ddlRows) ? (ddlRows as Record<string, unknown>[]) : [])[0]
    const ddl = String(ddlRow?.['Create Table'] ?? '')

    return { columns, indexes, ddl }
  }
```

import に `TableSchema` を追加（`from '../../shared/types'`）。

- [ ] **Step 4: テストが通ることを確認**

Run: `npm test -- ConnectionManager.schema`
Expected: PASS（3 tests）

- [ ] **Step 5: Commit**

```bash
git add src/main/connection/ConnectionManager.ts src/main/connection/ConnectionManager.schema.test.ts
git commit -m "feat: ConnectionManager にテーブル構造取得 tableSchema を実装"
```

### Task 1-3: IPC + preload の配線

**Files:**
- Modify: `src/main/ipc/registerDbHandlers.ts`
- Modify: `src/preload/index.ts`
- Modify: `src/renderer/src/env.d.ts`（window.api の型が手書きならそこにも。`Api` 型 export を使っているなら不要）

- [ ] **Step 1: IPC ハンドラ追加**

`registerDbHandlers.ts` の `db:autoIncrementColumns` ハンドラの直後に、同一パターンで:

```ts
  ipcMain.handle(
    'db:tableSchema',
    async (_e, table: string): Promise<ApiResult<TableSchema>> => {
      try {
        return { ok: true, data: await manager.tableSchema(table) }
      } catch (err) {
        return { ok: false, error: normalizeDbError(err) }
      }
    }
  )
```

import に `TableSchema` を追加。

- [ ] **Step 2: preload に bridge 追加**

`src/preload/index.ts` の `autoIncrementColumns` の直後に:

```ts
  tableSchema: (table: string): Promise<ApiResult<TableSchema>> =>
    ipcRenderer.invoke('db:tableSchema', table),
```

import に `TableSchema` を追加。

- [ ] **Step 3: typecheck + 全テスト**

Run: `npm run typecheck && npm test` → PASS

- [ ] **Step 4: Commit**

```bash
git add src/main/ipc/registerDbHandlers.ts src/preload/index.ts
git commit -m "feat: tableSchema の IPC ハンドラと preload bridge を追加"
```

### Task 1-4: ストアにビュー切り替え状態とスキーマ取得アクションを追加

**Files:**
- Modify: `src/renderer/src/store/useAppStore.ts`

- [ ] **Step 1: TableTab 型と初期値の拡張**

`useAppStore.ts` の `TableTab` interface（40 行目付近）にフィールド追加:

```ts
  view: 'data' | 'structure' // 表示モード。既定 'data'
  schema: TableSchema | null // 構造ビュー用。未取得は null（lazy load）
  schemaError: AppError | null
```

`makeTableTab()`（85 行目付近）の戻り値に `view: 'data', schema: null, schemaError: null` を追加。import に `TableSchema` を追加。

- [ ] **Step 2: アクション追加**

AppState の actions 群に追加（既存の table 系アクションの実装パターン: `get().tabs.find((t): t is TableTab => t.id === tabId && t.kind === 'table')` を踏襲）:

```ts
  // 表示モードを切り替える。structure へ切り替え時、未取得ならスキーマを lazy load する
  setTableView: async (tabId: string, view: 'data' | 'structure'): Promise<void> => {
    const tab = get().tabs.find((t): t is TableTab => t.id === tabId && t.kind === 'table')
    if (!tab) return
    set({ tabs: get().tabs.map((t) => (t.id === tabId && t.kind === 'table' ? { ...t, view } : t)) })
    if (view === 'structure' && !tab.schema) {
      const res = await window.api.tableSchema(tab.tableName)
      set({
        tabs: get().tabs.map((t) =>
          t.id === tabId && t.kind === 'table'
            ? res.ok
              ? { ...t, schema: res.data, schemaError: null }
              : { ...t, schemaError: res.error }
            : t
        )
      })
    }
  },
```

注意: Cmd+R（reload）でテーブルを再読込するアクションがある場合、そこで `schema: null` にリセットして次回表示時に再取得させること（ALTER 後の陳腐化対策）。

- [ ] **Step 3: typecheck**

Run: `npm run typecheck` → PASS

- [ ] **Step 4: Commit**

```bash
git add src/renderer/src/store/useAppStore.ts
git commit -m "feat: テーブルタブに data/structure ビュー状態とスキーマ取得を追加"
```

### Task 1-5: SchemaView コンポーネントとツールバー切り替え

**Files:**
- Create: `src/renderer/src/workspace/SchemaView.tsx`
- Create: `src/renderer/src/workspace/SchemaView.module.css`
- Modify: `src/renderer/src/workspace/WorkspaceShell.tsx`（テーブルタブのコンテンツ分岐）
- Modify: グリッドツールバー（split toggle ボタンがあるコンポーネント。WorkspaceShell.tsx 内のツールバー領域 — PR #21 で「grid toolbar」に統合済み）

- [ ] **Step 1: SchemaView.tsx を作成**

```tsx
import { useAppStore } from '../store/useAppStore'
import type { TableTab } from '../store/useAppStore'
import styles from './SchemaView.module.css'

export default function SchemaView({ tab }: { tab: TableTab }): JSX.Element {
  if (tab.schemaError) return <div className={styles.error}>{tab.schemaError.message}</div>
  if (!tab.schema) return <div className={styles.loading}>読み込み中…</div>
  const { columns, indexes, ddl } = tab.schema
  return (
    <div className={styles.root}>
      <section>
        <h3 className={styles.heading}>Columns</h3>
        <table className={styles.table}>
          <thead>
            <tr><th>Name</th><th>Type</th><th>Null</th><th>Key</th><th>Default</th><th>Extra</th><th>Comment</th></tr>
          </thead>
          <tbody>
            {columns.map((c) => (
              <tr key={c.name}>
                <td>{c.name}</td><td>{c.type}</td><td>{c.nullable ? 'YES' : 'NO'}</td>
                <td>{c.key}</td><td>{c.default ?? <span className={styles.null}>NULL</span>}</td>
                <td>{c.extra}</td><td>{c.comment}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </section>
      <section>
        <h3 className={styles.heading}>Indexes</h3>
        <table className={styles.table}>
          <thead><tr><th>Name</th><th>Columns</th><th>Unique</th></tr></thead>
          <tbody>
            {indexes.map((i) => (
              <tr key={i.name}><td>{i.name}</td><td>{i.columns.join(', ')}</td><td>{i.unique ? 'YES' : ''}</td></tr>
            ))}
          </tbody>
        </table>
      </section>
      <section>
        <h3 className={styles.heading}>DDL</h3>
        <pre className={styles.ddl}>{ddl}</pre>
      </section>
    </div>
  )
}
```

CSS は既存の DetailPane.module.css / ResultsGrid.module.css の配色・余白に合わせる（スクロール可能な `overflow: auto`、`font-size` はグリッドと同等、DDL は monospace）。

- [ ] **Step 2: WorkspaceShell の分岐とトグルボタン**

テーブルタブのグリッド描画箇所で `tab.view === 'structure'` なら `<SchemaView tab={tab} />`、それ以外は既存のグリッド（split view 含む）を描画。グリッドツールバー（split toggle のある場所）に「データ / 構造」のセグメントトグル（または構造アイコンボタン）を追加し、`setTableView(tab.id, ...)` を呼ぶ。既存のアイコンボタンの実装（SVG + 固定サイズ、PR #21 のスタイル）に合わせること。構造ビュー表示中は Pager / FilterBar など データ専用 UI を非表示にする。

- [ ] **Step 3: 動作確認**

Run: `npm run dev` → テーブルを開き、トグルで構造ビューに切り替え、カラム・インデックス・DDL が表示されること。データに戻れること。

- [ ] **Step 4: typecheck + テスト + Commit**

```bash
npm run typecheck && npm test
git add src/renderer/src/workspace/SchemaView.tsx src/renderer/src/workspace/SchemaView.module.css src/renderer/src/workspace/WorkspaceShell.tsx
git commit -m "feat: テーブル構造（Structure）ビューを追加"
```

- [ ] **Step 5: PR 作成 → squash マージ**

```bash
git push -u origin feature/schema-view
gh pr create --title "feat: テーブル構造表示ビュー" --body "..."
```

---

# Phase 2: SQL エディタのテーブル名・カラム名補完

`@codemirror/lang-sql` の `schema` オプションにテーブル→カラムのマップを渡すだけで補完が効く。接続時に `information_schema` から一括取得する。

### Task 2-1: `ConnectionManager.schemaMap()` の実装（TDD）

**Files:**
- Modify: `src/main/connection/ConnectionManager.ts`
- Test: `src/main/connection/ConnectionManager.schema.test.ts`（Phase 1 のファイルに追記）

- [ ] **Step 1: 失敗するテストを追記**

```ts
  it('schemaMap が接続中 DB のテーブル→カラム一覧を返す', async () => {
    const map = await manager.schemaMap()
    expect(map.schema_test).toEqual(['id', 'email', 'age'])
  })
```

Run: `npm test -- ConnectionManager.schema` → FAIL

- [ ] **Step 2: 実装**

```ts
  // SQL 補完用：接続中 DB の { テーブル名: カラム名[] } を一括取得する。
  async schemaMap(): Promise<Record<string, string[]>> {
    if (!this.pool) throw new Error('Not connected')
    const [rows] = await this.pool.query(
      `SELECT TABLE_NAME AS t, COLUMN_NAME AS c
         FROM information_schema.COLUMNS
        WHERE TABLE_SCHEMA = DATABASE()
        ORDER BY TABLE_NAME, ORDINAL_POSITION`
    )
    const map: Record<string, string[]> = {}
    for (const r of Array.isArray(rows) ? (rows as Record<string, unknown>[]) : []) {
      const t = String(r.t)
      ;(map[t] ??= []).push(String(r.c))
    }
    return map
  }
```

- [ ] **Step 3: テスト PASS を確認 → Commit**

```bash
npm test -- ConnectionManager.schema
git add src/main/connection/ConnectionManager.ts src/main/connection/ConnectionManager.schema.test.ts
git commit -m "feat: SQL 補完用の schemaMap 取得を ConnectionManager に実装"
```

### Task 2-2: IPC / preload / ストアの配線

**Files:**
- Modify: `src/main/ipc/registerDbHandlers.ts`（`db:schemaMap` ハンドラ。Phase 1 Task 1-3 と同一パターン）
- Modify: `src/preload/index.ts`（`schemaMap: (): Promise<ApiResult<Record<string, string[]>>> => ipcRenderer.invoke('db:schemaMap')`）
- Modify: `src/renderer/src/store/useAppStore.ts`

- [ ] **Step 1: IPC + preload を追加**（Phase 1 と同型のため省略せず実装すること）

- [ ] **Step 2: ストアに `schemaMap` state を追加**

AppState に `schemaMap: Record<string, string[]>`（初期値 `{}`）を追加。接続成功後にテーブル一覧を取得している箇所（`connections:connect` 成功後、`listTables` を呼んでいるアクション）で続けて:

```ts
    const sm = await window.api.schemaMap()
    set({ schemaMap: sm.ok ? sm.data : {} })
```

切断・接続一覧へ戻る処理で `schemaMap: {}` にリセットする。

- [ ] **Step 3: typecheck → Commit**

```bash
npm run typecheck && npm test
git add -A && git commit -m "feat: schemaMap の IPC 配線とストア保持を追加"
```

### Task 2-3: QueryEditor に補完を組み込む

**Files:**
- Modify: `src/renderer/src/workspace/QueryEditor.tsx`

- [ ] **Step 1: schema オプションを渡す**

`QueryEditor.tsx:20-38` の `useMemo` を変更。**注意: extensions の安定参照コメント（打鍵ごと reconfigure 防止）を守るため、deps は `[runActiveTab, schemaMap]` とし、schemaMap は接続後に一度しか変わらないことを確認する。**

```tsx
const schemaMap = useAppStore((s) => s.schemaMap)

const extensions = useMemo(
  () => [
    sql({ dialect: MySQL, schema: schemaMap, upperCaseKeywords: true }),
    Prec.highest(
      keymap.of([
        {
          key: 'Mod-Enter',
          run: () => {
            void runActiveTab()
            return true
          }
        }
      ])
    )
  ],
  [runActiveTab, schemaMap]
)
```

`basicSetup` は autocompletion を既定で含む（@uiw/react-codemirror の basicSetup デフォルト）ので、追加パッケージは不要。補完が出ない場合のみ `basicSetup={{ lineNumbers: true, highlightActiveLine: true, autocompletion: true }}` を明示する。

- [ ] **Step 2: 動作確認**

Run: `npm run dev` → SQL タブで `SELECT * FROM ` まで打つとテーブル名が補完候補に出る。`SELECT schema_test.` でカラム名が出る。

- [ ] **Step 3: typecheck + Commit + PR**

```bash
npm run typecheck && npm test
git add src/renderer/src/workspace/QueryEditor.tsx
git commit -m "feat: SQL エディタにテーブル名・カラム名補完を追加"
git push -u origin feature/sql-completion && gh pr create --title "feat: SQL 補完" --body "..."
```

---

# Phase 3: クエリ履歴

実行した SQL を main 側で JSON ファイルに永続化し、SQL タブから履歴パネルで検索・再利用できるようにする。

### Task 3-1: 共有型と `QueryHistoryStore`（TDD）

**Files:**
- Modify: `src/shared/types.ts`
- Create: `src/main/history/QueryHistoryStore.ts`
- Test: `src/main/history/QueryHistoryStore.test.ts`

- [ ] **Step 1: 型を追加（types.ts 末尾）**

```ts
// クエリ履歴の1エントリ（main 側で userData に永続化）
export interface QueryHistoryEntry {
  id: string
  sql: string
  executedAt: string // ISO 8601
  durationMs: number
  ok: boolean
  errorMessage?: string
  database?: string // 実行時の接続先 DB 名
}
```

- [ ] **Step 2: 失敗するテストを書く**

ProfileStore のテストの流儀（テンポラリディレクトリにファイルを書く）に合わせる:

```ts
import { describe, it, expect, beforeEach, afterEach } from 'vitest'
import { mkdtempSync, rmSync } from 'node:fs'
import { tmpdir } from 'node:os'
import { join } from 'node:path'
import { QueryHistoryStore } from './QueryHistoryStore'

describe('QueryHistoryStore', () => {
  let dir: string
  beforeEach(() => {
    dir = mkdtempSync(join(tmpdir(), 'qh-'))
  })
  afterEach(() => rmSync(dir, { recursive: true, force: true }))

  it('add した履歴を新しい順で list できる', () => {
    const store = new QueryHistoryStore(dir)
    store.add({ sql: 'SELECT 1', durationMs: 5, ok: true, database: 'test' })
    store.add({ sql: 'SELECT 2', durationMs: 7, ok: true, database: 'test' })
    const list = store.list()
    expect(list).toHaveLength(2)
    expect(list[0].sql).toBe('SELECT 2') // 新しい順
    expect(list[0].id).toBeTruthy()
    expect(list[0].executedAt).toBeTruthy()
  })

  it('上限 500 件を超えると古いものから捨てる', () => {
    const store = new QueryHistoryStore(dir)
    for (let i = 0; i < 510; i++) store.add({ sql: `SELECT ${i}`, durationMs: 1, ok: true })
    const list = store.list()
    expect(list).toHaveLength(500)
    expect(list[0].sql).toBe('SELECT 509')
    expect(list[499].sql).toBe('SELECT 10')
  })

  it('別インスタンスで読み直せる（永続化）', () => {
    new QueryHistoryStore(dir).add({ sql: 'SELECT 1', durationMs: 5, ok: true })
    expect(new QueryHistoryStore(dir).list()).toHaveLength(1)
  })

  it('clear で全削除できる', () => {
    const store = new QueryHistoryStore(dir)
    store.add({ sql: 'SELECT 1', durationMs: 5, ok: true })
    store.clear()
    expect(store.list()).toHaveLength(0)
  })
})
```

Run: `npm test -- QueryHistoryStore` → FAIL

- [ ] **Step 3: 実装**

```ts
import { readFileSync, writeFileSync, existsSync, mkdirSync } from 'node:fs'
import { join } from 'node:path'
import { randomUUID } from 'node:crypto'
import type { QueryHistoryEntry } from '../../shared/types'

const MAX_ENTRIES = 500

// SQL 実行履歴を userData 配下の JSON に永続化する。新しい順で保持。
export class QueryHistoryStore {
  private readonly filePath: string
  private entries: QueryHistoryEntry[]

  constructor(baseDir: string) {
    mkdirSync(baseDir, { recursive: true })
    this.filePath = join(baseDir, 'query-history.json')
    this.entries = this.load()
  }

  private load(): QueryHistoryEntry[] {
    if (!existsSync(this.filePath)) return []
    try {
      const parsed = JSON.parse(readFileSync(this.filePath, 'utf-8'))
      return Array.isArray(parsed) ? parsed : []
    } catch {
      return [] // 壊れたファイルは捨てて空から始める
    }
  }

  private persist(): void {
    writeFileSync(this.filePath, JSON.stringify(this.entries))
  }

  add(input: Omit<QueryHistoryEntry, 'id' | 'executedAt'>): QueryHistoryEntry {
    const entry: QueryHistoryEntry = { ...input, id: randomUUID(), executedAt: new Date().toISOString() }
    this.entries = [entry, ...this.entries].slice(0, MAX_ENTRIES)
    this.persist()
    return entry
  }

  list(): QueryHistoryEntry[] {
    return this.entries
  }

  clear(): void {
    this.entries = []
    this.persist()
  }
}
```

- [ ] **Step 4: テスト PASS → Commit**

```bash
npm test -- QueryHistoryStore
git add src/shared/types.ts src/main/history/
git commit -m "feat: クエリ履歴の永続化ストア QueryHistoryStore を実装"
```

### Task 3-2: 実行時の記録と IPC

**Files:**
- Modify: `src/main/index.ts`（QueryHistoryStore のインスタンス化。ProfileStore と同様に `app.getPath('userData')` を渡す）
- Modify: `src/main/ipc/registerDbHandlers.ts`
- Modify: `src/preload/index.ts`

- [ ] **Step 1: `db:queryScript` ハンドラで記録する**

`registerDbHandlers` のシグネチャを `registerDbHandlers(manager: ConnectionManager, history: QueryHistoryStore)` に変更し、`main/index.ts` の呼び出し側も合わせる。`db:queryScript` ハンドラを変更:

```ts
  ipcMain.handle(
    'db:queryScript',
    async (_e, sql: string): Promise<ApiResult<QueryResult>> => {
      try {
        const data = await manager.queryScript(sql)
        history.add({ sql, durationMs: data.durationMs, ok: true })
        return { ok: true, data }
      } catch (err) {
        const error = normalizeDbError(err)
        history.add({ sql, durationMs: 0, ok: false, errorMessage: error.message })
        return { ok: false, error }
      }
    }
  )
```

注: `database` フィールドは接続中プロファイルを IPC ハンドラ層が知らないため v0.2.0 では省略してよい（入れる場合は main/index.ts で現在接続中の DB 名を共有する）。

- [ ] **Step 2: 履歴の IPC + preload**

```ts
// registerDbHandlers.ts（または新規 registerHistoryHandlers.ts — ファイル分割は IPC ハンドラ群の既存粒度に合わせて判断）
  ipcMain.handle('history:list', async (): Promise<ApiResult<QueryHistoryEntry[]>> => {
    return { ok: true, data: history.list() }
  })
  ipcMain.handle('history:clear', async (): Promise<ApiResult<null>> => {
    history.clear()
    return { ok: true, data: null }
  })
```

```ts
// preload/index.ts の api に追加
  history: {
    list: (): Promise<ApiResult<QueryHistoryEntry[]>> => ipcRenderer.invoke('history:list'),
    clear: (): Promise<ApiResult<null>> => ipcRenderer.invoke('history:clear')
  },
```

- [ ] **Step 3: typecheck + テスト → Commit**

```bash
npm run typecheck && npm test
git add -A && git commit -m "feat: クエリ実行時の履歴記録と履歴 IPC を追加"
```

### Task 3-3: 履歴パネル UI

**Files:**
- Create: `src/renderer/src/workspace/HistoryPanel.tsx`
- Create: `src/renderer/src/workspace/HistoryPanel.module.css`
- Modify: `src/renderer/src/workspace/WorkspaceShell.tsx`（SQL タブ表示時のレイアウトに組み込み）
- Modify: `src/renderer/src/store/useAppStore.ts`（`historyOpen: boolean` とトグルアクション）

- [ ] **Step 1: ストアに状態追加**

`historyOpen: boolean`（初期値 false）と `toggleHistory: () => set({ historyOpen: !get().historyOpen })` を追加。

- [ ] **Step 2: HistoryPanel.tsx**

```tsx
import { useEffect, useState } from 'react'
import { useAppStore } from '../store/useAppStore'
import type { QueryHistoryEntry } from '../../../shared/types'
import styles from './HistoryPanel.module.css'

export default function HistoryPanel(): JSX.Element {
  const [entries, setEntries] = useState<QueryHistoryEntry[]>([])
  const [filter, setFilter] = useState('')
  const activeTabId = useAppStore((s) => s.activeTabId)
  const setTabSql = useAppStore((s) => s.setTabSql)
  const toggleHistory = useAppStore((s) => s.toggleHistory)

  useEffect(() => {
    void window.api.history.list().then((res) => {
      if (res.ok) setEntries(res.data)
    })
  }, [])

  const visible = filter
    ? entries.filter((e) => e.sql.toLowerCase().includes(filter.toLowerCase()))
    : entries

  return (
    <aside className={styles.panel}>
      <div className={styles.header}>
        <input
          className={styles.search}
          placeholder="履歴を検索"
          value={filter}
          onChange={(e) => setFilter(e.target.value)}
        />
        <button className={styles.close} onClick={toggleHistory} aria-label="閉じる">×</button>
      </div>
      <ul className={styles.list}>
        {visible.map((e) => (
          <li key={e.id}>
            <button
              className={e.ok ? styles.item : styles.itemError}
              title={e.sql}
              onClick={() => activeTabId && setTabSql(activeTabId, e.sql)}
            >
              <span className={styles.sql}>{e.sql}</span>
              <span className={styles.meta}>
                {new Date(e.executedAt).toLocaleString()} ・ {e.durationMs}ms{e.ok ? '' : ' ・ 失敗'}
              </span>
            </button>
          </li>
        ))}
        {visible.length === 0 && <li className={styles.empty}>履歴はありません</li>}
      </ul>
    </aside>
  )
}
```

クリックでアクティブ SQL タブのエディタ内容を置き換える（`setTabSql` は既存。アクティブタブが SQL タブでない場合はボタンを disabled にする）。

- [ ] **Step 3: WorkspaceShell への組み込み**

SQL タブ表示時、エディタ右側に `historyOpen && <HistoryPanel />` を表示（幅 280px 程度の固定サイドパネル、flex で横並び）。SQL タブのツールバー/ヒント行に履歴トグルボタン（時計アイコン）を追加。

- [ ] **Step 4: 動作確認 + Commit + PR**

Run: `npm run dev` → クエリを数回実行 → 履歴パネルを開く → エントリ表示・検索・クリックでエディタへ反映。

```bash
npm run typecheck && npm test
git add -A && git commit -m "feat: SQL タブにクエリ履歴パネルを追加"
git push -u origin feature/query-history && gh pr create --title "feat: クエリ履歴" --body "..."
```

---

# Phase 4: EXPLAIN 実行

SQL タブのクエリを `EXPLAIN` 付きで実行し、結果をそのまま結果グリッドに表示する。Cmd+E ショートカット + ツールバーボタン。

### Task 4-1: ストアアクション `explainActiveTab`

**Files:**
- Modify: `src/renderer/src/store/useAppStore.ts`
- Test: `src/renderer/src/store/explain.test.ts`（新規・純粋関数部分）

- [ ] **Step 1: 単一文判定の純粋関数とテスト**

EXPLAIN は単一文にのみ対応する（複数文は誤解を生むため明示エラー）。`src/renderer/src/store/explain.ts`:

```ts
// EXPLAIN 用：入力 SQL を単一文として取り出す。
// 末尾セミコロン・前後空白は許容。文中に ; が残る（=複数文）場合は null。
// 注: 文字列リテラル内の ; は誤判定するが、EXPLAIN 用途では許容する簡易実装。
export function singleStatementOf(sql: string): string | null {
  const trimmed = sql.trim().replace(/;\s*$/, '')
  if (trimmed.length === 0) return null
  if (trimmed.includes(';')) return null
  return trimmed
}
```

テスト `explain.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { singleStatementOf } from './explain'

describe('singleStatementOf', () => {
  it('末尾セミコロンを除去して返す', () => {
    expect(singleStatementOf('SELECT 1;')).toBe('SELECT 1')
  })
  it('セミコロン無しはそのまま', () => {
    expect(singleStatementOf('  SELECT 1  ')).toBe('SELECT 1')
  })
  it('複数文は null', () => {
    expect(singleStatementOf('SELECT 1; SELECT 2;')).toBeNull()
  })
  it('空文字は null', () => {
    expect(singleStatementOf('  ;  ')).toBeNull()
  })
})
```

Run: `npm test -- explain` → 実装前 FAIL → 実装 → PASS

- [ ] **Step 2: ストアアクション**

既存の `runActiveTab`（453 行目付近）の実装を参考に:

```ts
  explainActiveTab: async (): Promise<void> => {
    const tab = get().tabs.find((t) => t.id === get().activeTabId)
    if (!tab || tab.kind !== 'sql') return
    const stmt = singleStatementOf(tab.sql)
    if (!stmt) {
      set({
        tabs: get().tabs.map((t) =>
          t.id === tab.id
            ? { ...t, error: { code: 'EXPLAIN_MULTI', message: 'EXPLAIN は単一文のみ実行できます' } }
            : t
        )
      })
      return
    }
    // 以降は runSql と同じ running → result/error 反映パターンで api.query(`EXPLAIN ${stmt}`) を実行する
    // （既存の runSql ヘルパーが流用できるならそれに `EXPLAIN ${stmt}` を渡すだけでよい）
  },
```

- [ ] **Step 3: UI: Cmd+E とボタン**

`QueryEditor.tsx` の keymap に `{ key: 'Mod-e', run: () => { void explainActiveTab(); return true } }` を追加（既存 Mod-Enter と同じ `Prec.highest` ブロック内）。ヒント行を `⌘↵ で実行 ・ ⌘E で EXPLAIN` に更新。

- [ ] **Step 4: 動作確認 + Commit + PR**

Run: `npm run dev` → `SELECT * FROM <table>` で Cmd+E → EXPLAIN 結果（id / select_type / table / type / key / rows / Extra 列）がグリッドに出る。複数文ではエラーメッセージが出る。

```bash
npm run typecheck && npm test
git add -A && git commit -m "feat: SQL タブに EXPLAIN 実行（Cmd+E）を追加"
git push -u origin feature/explain && gh pr create --title "feat: EXPLAIN 実行" --body "..."
```

---

# Phase 5: SQL タブ結果の CSV エクスポート

テーブルタブの CSV エクスポート（`toCsv` + `file:saveCsv` IPC、`ExportMenu.tsx`）は実装済み。SQL タブの結果にも同じ導線を追加する。**main 側の追加実装は不要**（`api.saveCsv` を再利用）。

### Task 5-1: ストアアクションと UI

**Files:**
- Modify: `src/renderer/src/store/useAppStore.ts`
- Modify: SQL タブの結果表示まわり（StatusBar.tsx または ExportMenu.tsx — 既存 ExportMenu がテーブルタブ専用ならば SQL タブでも表示されるよう条件を緩める方針が第一候補）

- [ ] **Step 1: ストアに `exportSqlResultCsv` を追加**

既存の `exportCsv`（テーブルタブ用）の実装を読み、ファイル名生成・`toCsv` 呼び出し・`ExportCsvResult` の返し方を踏襲する:

```ts
  // SQL タブの現在の結果を CSV 保存する。結果が無ければ何もしない。
  exportSqlResultCsv: async (tabId: string): Promise<ExportCsvResult> => {
    const tab = get().tabs.find((t) => t.id === tabId)
    if (!tab || tab.kind !== 'sql' || !tab.result || tab.result.rows.length === 0) {
      return { ok: false, message: 'エクスポートする結果がありません' }
    }
    const columns = tab.result.columns.map((c) => c.name)
    const csv = toCsv(columns, tab.result.rows)
    const res = await window.api.saveCsv(`${tab.title}.csv`, csv)
    if (!res.ok) return { ok: false, message: res.error.message }
    if (res.data.canceled) return { ok: true, canceled: true, message: '' }
    return { ok: true, message: `CSV を保存しました: ${res.data.filePath}` }
  },
```

注: `toCsv` のシグネチャは `src/renderer/src/lib/csv.ts` を読んで合わせること（BOM 付与の有無も既存実装に従う）。

- [ ] **Step 2: UI 導線**

SQL タブの結果グリッド上部（または StatusBar）に「CSV エクスポート」ボタンを追加し、`exportSqlResultCsv(tab.id)` → 結果メッセージを既存のトースト通知の仕組みで表示する。結果 0 件時は disabled。

- [ ] **Step 3: 動作確認 + Commit + PR**

Run: `npm run dev` → SQL タブで SELECT 実行 → エクスポート → 保存した CSV を開いてヘッダ + 行が入っていること。

```bash
npm run typecheck && npm test
git add -A && git commit -m "feat: SQL タブの実行結果を CSV エクスポート可能にする"
git push -u origin feature/sql-result-csv && gh pr create --title "feat: SQL 結果の CSV エクスポート" --body "..."
```

---

# Phase 6: セルインスペクタ強化（JSON 整形表示）

詳細ペイン（DetailPane）で JSON 値を整形表示できるようにする。

### Task 6-1: `tryFormatJson` ユーティリティ（TDD）

**Files:**
- Create: `src/renderer/src/lib/formatJson.ts`
- Test: `src/renderer/src/lib/formatJson.test.ts`

- [ ] **Step 1: 失敗するテスト**

```ts
import { describe, it, expect } from 'vitest'
import { tryFormatJson } from './formatJson'

describe('tryFormatJson', () => {
  it('JSON オブジェクトを 2 スペースで整形する', () => {
    expect(tryFormatJson('{"a":1,"b":[2,3]}')).toBe('{\n  "a": 1,\n  "b": [\n    2,\n    3\n  ]\n}')
  })
  it('JSON 配列も整形する', () => {
    expect(tryFormatJson('[1,2]')).toBe('[\n  1,\n  2\n]')
  })
  it('プリミティブ（数値・文字列）は null（整形対象外）', () => {
    expect(tryFormatJson('123')).toBeNull()
    expect(tryFormatJson('"hello"')).toBeNull()
  })
  it('JSON でない文字列は null', () => {
    expect(tryFormatJson('hello world')).toBeNull()
    expect(tryFormatJson('')).toBeNull()
  })
})
```

Run: `npm test -- formatJson` → FAIL

- [ ] **Step 2: 実装**

```ts
// 値が JSON のオブジェクト/配列なら 2 スペースインデントで整形して返す。
// プリミティブや非 JSON は null（整形表示の対象外）。
export function tryFormatJson(value: string): string | null {
  const trimmed = value.trim()
  if (!trimmed.startsWith('{') && !trimmed.startsWith('[')) return null
  try {
    return JSON.stringify(JSON.parse(trimmed), null, 2)
  } catch {
    return null
  }
}
```

- [ ] **Step 3: テスト PASS → Commit**

```bash
npm test -- formatJson
git add src/renderer/src/lib/formatJson.ts src/renderer/src/lib/formatJson.test.ts
git commit -m "feat: JSON 整形ユーティリティ tryFormatJson を追加"
```

### Task 6-2: DetailPane への組み込み

**Files:**
- Modify: `src/renderer/src/workspace/DetailPane.tsx`
- Modify: `src/renderer/src/workspace/DetailPane.module.css`

- [ ] **Step 1: 整形表示トグルを追加**

DetailPane の各フィールド描画部（編集 input と型表示 `col.type` がある箇所）で、値（文字列化したもの）に対して `tryFormatJson` が non-null の場合のみ「{} 整形」トグルボタンを表示する。トグル ON のとき、編集 input の下に読み取り専用の整形ビューを出す:

```tsx
{formatted !== null && expanded && (
  <pre className={styles.jsonView}>{formatted}</pre>
)}
```

CSS（DetailPane.module.css に追加）:

```css
.jsonView {
  margin: 4px 0 0;
  padding: 8px;
  max-height: 240px;
  overflow: auto;
  font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
  font-size: 12px;
  background: var(--panel-bg, #f6f6f6);
  border: 1px solid var(--border, #e0e0e0);
  border-radius: 4px;
  white-space: pre;
}
```

トグル状態はコンポーネントローカル state（`useState<Record<string, boolean>>` で列名→展開状態）でよい。**編集機能（既存の input / NULL セット）は一切変更しない** — 整形ビューはあくまで読み取り専用の追加表示。

- [ ] **Step 2: 動作確認 + Commit + PR**

Run: `npm run dev` → JSON カラム（または JSON 文字列が入った TEXT カラム）のある行を選択 → 詳細ペインで整形トグルが出る → 整形表示される。非 JSON 値の列にはトグルが出ない。

```bash
npm run typecheck && npm test
git add -A && git commit -m "feat: 詳細ペインに JSON 整形表示を追加"
git push -u origin feature/json-inspector && gh pr create --title "feat: JSON インスペクタ" --body "..."
```

---

# Phase 7: ダークモード（OS 外観に追従）

macOS の外観設定（ライト/ダーク）に自動追従する。手動切り替えは v0.2.0 ではスコープ外（OS 追従のみ）。

### Task 7-1: CSS 変数化とダークパレット定義

**Files:**
- Modify: `src/renderer/src/theme.css`
- Modify: `src/renderer/src/**/*.module.css`（ハードコード色の変数置換）

- [ ] **Step 1: 現状の色の棚卸し**

```bash
grep -rhoE '#[0-9a-fA-F]{3,8}|rgba?\([^)]*\)' src/renderer/src --include='*.css' | sort | uniq -c | sort -rn
```

出現する色を「背景 / パネル背景 / ボーダー / 主要テキスト / 補助テキスト / アクセント / 危険 / 選択行 / ホバー」程度の意味カテゴリに分類する。

- [ ] **Step 2: theme.css に変数とダーク値を定義**

`theme.css` の先頭に（既存の値を `:root` のライト値として移す。以下は構造のサンプルであり、実際の値は Step 1 の棚卸し結果を使うこと）:

```css
:root {
  color-scheme: light dark; /* ネイティブ部品（スクロールバー等）も追従させる */
  --bg: #ffffff;
  --panel-bg: #f6f6f6;
  --border: #e0e0e0;
  --text: #1d1d1f;
  --text-muted: #6e6e73;
  --accent: #0a66c2;
  --danger: #d93025;
  --row-selected: #e8f0fe;
  --row-hover: #f2f2f2;
}

@media (prefers-color-scheme: dark) {
  :root {
    --bg: #1e1e1e;
    --panel-bg: #252526;
    --border: #3c3c3c;
    --text: #e4e4e4;
    --text-muted: #9d9d9d;
    --accent: #4d9fff;
    --danger: #ff6b5e;
    --row-selected: #1f3a5f;
    --row-hover: #2a2a2a;
  }
}
```

- [ ] **Step 3: module.css のハードコード色を変数に置換**

各 `*.module.css` の色指定を `var(--...)` に機械的に置換する。1 ファイルずつ置換 → `npm run dev` で見た目が崩れていないか確認。**この置換はファイル数が多いので、1 コミットにまとめず「ワークスペース系」「ホーム系」「コンポーネント系」程度の単位でコミットを分ける。**

- [ ] **Step 4: Commit**

```bash
git add src/renderer/src
git commit -m "refactor: 配色を CSS 変数化しダークパレットを定義"
```

### Task 7-2: CodeMirror のテーマ追従

**Files:**
- Create: `src/renderer/src/lib/useColorScheme.ts`
- Modify: `src/renderer/src/workspace/QueryEditor.tsx`

- [ ] **Step 1: useColorScheme フック**

```ts
import { useEffect, useState } from 'react'

// OS のライト/ダーク設定に追従する。変更はリアルタイム反映。
export function useColorScheme(): 'light' | 'dark' {
  const [scheme, setScheme] = useState<'light' | 'dark'>(() =>
    window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light'
  )
  useEffect(() => {
    const mq = window.matchMedia('(prefers-color-scheme: dark)')
    const handler = (e: MediaQueryListEvent): void => setScheme(e.matches ? 'dark' : 'light')
    mq.addEventListener('change', handler)
    return () => mq.removeEventListener('change', handler)
  }, [])
  return scheme
}
```

- [ ] **Step 2: QueryEditor の `theme="light"` 固定を置換**

`QueryEditor.tsx:48` の `theme="light"` を:

```tsx
const scheme = useColorScheme()
// ...
<CodeMirror theme={scheme} ... />
```

- [ ] **Step 3: 動作確認**

Run: `npm run dev` → macOS のシステム設定 > 外観 をダークに切り替え → アプリ全体と SQL エディタが即時ダークになる。ライトに戻す → 戻る。グリッド・詳細ペイン・接続一覧・モーダル・コンテキストメニューを一通り目視確認。

- [ ] **Step 4: Commit + PR**

```bash
npm run typecheck && npm test
git add -A && git commit -m "feat: ダークモード（OS 外観追従）に対応"
git push -u origin feature/dark-mode && gh pr create --title "feat: ダークモード" --body "..."
```

---

# Phase 8: SSH トンネル接続

踏み台（bastion）経由で staging/production の MySQL に接続できるようにする。方式: main プロセス内で `127.0.0.1:0`（空きポート）に TCP サーバを立て、受けたソケットを ssh2 の `forwardOut` ストリームに接続する。mysql2 のプールはローカルポートに接続するだけなので、複数コネクションでも安全に動く。

認証は **パスワード認証 + 秘密鍵ファイル（パスフレーズ対応）** の両対応。SSH パスワード/パスフレーズは DB パスワードと同様に `safeStorage` で暗号化保存する。

### Task 8-1: 依存追加と共有型の拡張

**Files:**
- Modify: `package.json`
- Modify: `src/shared/types.ts`

- [ ] **Step 1: 依存を追加**

```bash
npm install ssh2
npm install -D @types/ssh2
```

注: ssh2 はネイティブバイナリ無しでも動く（optional の cpu-features が入らなくても可）。`npm run dist:mac` 後に DMG でも動作確認すること。

- [ ] **Step 2: 型の拡張（types.ts）**

```ts
// SSH トンネル設定。enabled=false なら他フィールドは無視される
export interface SshSettings {
  enabled: boolean
  host: string
  port: number // 既定 22
  user: string
  authMethod: 'password' | 'privateKey'
  password?: string // authMethod=password 時。保存時は暗号化される
  privateKeyPath?: string // authMethod=privateKey 時の鍵ファイルパス
  passphrase?: string // 鍵のパスフレーズ。保存時は暗号化される
}
```

`ConnectionConfig` / `ConnectionProfile` / `ConnectionProfileInput` にそれぞれ `ssh?: SshSettings` を追加。**`ConnectionProfile`（renderer へ渡す形）では `password` と `passphrase` を除いた形にすること**（既存の「パスワードは renderer に返さない」方針と同じ。型としては `ssh?: Omit<SshSettings, 'password' | 'passphrase'>` を別名 `SshSettingsPublic` として定義する）。

- [ ] **Step 3: typecheck → Commit**

```bash
npm run typecheck
git add package.json package-lock.json src/shared/types.ts
git commit -m "feat: ssh2 依存と SSH トンネル設定型を追加"
```

### Task 8-2: `SshTunnel` クラス（ローカル TCP → forwardOut 中継）

**Files:**
- Create: `src/main/connection/SshTunnel.ts`
- Test: `src/main/connection/SshTunnel.test.ts`

- [ ] **Step 1: 実装**

```ts
import { Client, type ConnectConfig } from 'ssh2'
import { createServer, type Server, type Socket } from 'node:net'
import { readFileSync } from 'node:fs'
import type { SshSettings } from '../../shared/types'

// SSH トンネル: 127.0.0.1 の空きポートで listen し、着信ソケットを
// ssh forwardOut で (dbHost:dbPort) へ中継する。mysql2 プールの複数接続にも対応。
export class SshTunnel {
  private client: Client | null = null
  private server: Server | null = null

  // トンネルを開き、mysql2 が接続すべきローカルポート番号を返す。
  async open(ssh: SshSettings, dbHost: string, dbPort: number): Promise<number> {
    const client = new Client()
    const config: ConnectConfig = {
      host: ssh.host,
      port: ssh.port,
      username: ssh.user,
      readyTimeout: 10_000
    }
    if (ssh.authMethod === 'password') {
      config.password = ssh.password ?? ''
    } else {
      config.privateKey = readFileSync(ssh.privateKeyPath ?? '')
      if (ssh.passphrase) config.passphrase = ssh.passphrase
    }

    await new Promise<void>((resolve, reject) => {
      client.once('ready', () => resolve())
      client.once('error', (err) => reject(err))
      client.connect(config)
    })
    this.client = client

    const server = createServer((socket: Socket) => {
      client.forwardOut(socket.remoteAddress ?? '127.0.0.1', socket.remotePort ?? 0, dbHost, dbPort, (err, stream) => {
        if (err) {
          socket.destroy()
          return
        }
        socket.pipe(stream).pipe(socket)
        stream.on('error', () => socket.destroy())
        socket.on('error', () => stream.destroy())
      })
    })
    this.server = server

    return new Promise<number>((resolve, reject) => {
      server.once('error', reject)
      server.listen(0, '127.0.0.1', () => {
        const addr = server.address()
        if (addr && typeof addr === 'object') resolve(addr.port)
        else reject(new Error('Failed to bind local tunnel port'))
      })
    })
  }

  async close(): Promise<void> {
    if (this.server) {
      await new Promise<void>((resolve) => this.server!.close(() => resolve()))
      this.server = null
    }
    if (this.client) {
      this.client.end()
      this.client = null
    }
  }

  isOpen(): boolean {
    return this.client !== null
  }
}
```

- [ ] **Step 2: ユニットテスト（SSH サーバ無しで検証できる範囲）**

実 SSH サーバを CI に立てるのは v0.2.0 ではやらない。テストは「接続失敗が reject されること」と「close が冪等であること」に限定する:

```ts
import { describe, it, expect } from 'vitest'
import { SshTunnel } from './SshTunnel'

describe('SshTunnel', () => {
  it('到達不能ホストでは open が reject される', async () => {
    const tunnel = new SshTunnel()
    await expect(
      tunnel.open(
        { enabled: true, host: '127.0.0.1', port: 1, user: 'x', authMethod: 'password', password: 'x' },
        'db.example.com',
        3306
      )
    ).rejects.toThrow()
  }, 15_000)

  it('open していなくても close は安全に呼べる', async () => {
    await expect(new SshTunnel().close()).resolves.toBeUndefined()
  })
})
```

Run: `npm test -- SshTunnel` → PASS

- [ ] **Step 3: Commit**

```bash
git add src/main/connection/SshTunnel.ts src/main/connection/SshTunnel.test.ts
git commit -m "feat: SSH トンネル中継クラス SshTunnel を実装"
```

### Task 8-3: ProfileStore の SSH 設定保存（暗号化）

**Files:**
- Modify: `src/main/connection/ProfileStore.ts`
- Test: 既存の ProfileStore テストに追記

- [ ] **Step 1: 失敗するテストを追記**

既存テストのスタイル（safeStorage のモック方法を含む）に合わせて:

```ts
  it('SSH 設定を保存し、list では password/passphrase を返さない', () => {
    const saved = store.save({
      name: 'with-ssh', tag: 'staging', host: 'db', port: 3306, user: 'u', password: 'p',
      ssh: { enabled: true, host: 'bastion', port: 22, user: 'ec2-user', authMethod: 'password', password: 'sshpw' }
    })
    const listed = store.list().find((p) => p.id === saved.id)
    expect(listed?.ssh).toMatchObject({ enabled: true, host: 'bastion', user: 'ec2-user' })
    expect((listed?.ssh as Record<string, unknown>).password).toBeUndefined()
  })

  it('getFullConfig（接続用の内部取得）では SSH パスワードを復号して返す', () => {
    // 既存の「接続時にパスワードを復号して ConnectionConfig を組む」メソッド名に合わせること
  })
```

- [ ] **Step 2: 実装**

- 保存時: `ssh.password` / `ssh.passphrase` を DB パスワードと同じ `safeStorage.encryptString` → base64 で永続化（フィールド名は `sshPasswordEnc` / `sshPassphraseEnc` など、既存の DB パスワード暗号化フィールドの命名に合わせる）。
- `list()`（renderer 向け）: `ssh` から password/passphrase を除いた `SshSettingsPublic` を返す。
- 接続用の内部メソッド（既存で DB パスワードを復号して `ConnectionConfig` を返している箇所）: 復号した `ssh` をフルで返す。
- 既存プロファイル（ssh フィールド無し）の読み込みで壊れないこと（`ssh` は optional のまま扱う）。
- `duplicate()` でも ssh 設定（暗号化フィールド含む）を引き継ぐ。

- [ ] **Step 3: テスト PASS → Commit**

```bash
npm test -- ProfileStore
git add src/main/connection/ProfileStore.ts src/main/connection/*.test.ts
git commit -m "feat: 接続プロファイルに SSH 設定（暗号化保存）を追加"
```

### Task 8-4: 接続フローへの組み込み

**Files:**
- Modify: `src/main/ipc/registerConnectionHandlers.ts`（`connections:connect`）
- Modify: `src/main/ipc/registerDbHandlers.ts`（`db:connect` — フォームの Test Connection 用）
- Modify: `src/main/index.ts`（トンネルのライフサイクル管理）

- [ ] **Step 1: 接続時にトンネルを経由させる**

接続処理を共通化するヘルパーを作る（置き場所: `src/main/connection/connectWithTunnel.ts`）:

```ts
import { ConnectionManager } from './ConnectionManager'
import { SshTunnel } from './SshTunnel'
import type { ConnectionConfig } from '../../shared/types'

// ssh.enabled ならトンネルを張ってから 127.0.0.1:localPort へ接続する。
// 戻りのトンネルは切断時に close すること（不要なら null）。
export async function connectWithTunnel(
  manager: ConnectionManager,
  config: ConnectionConfig
): Promise<SshTunnel | null> {
  if (!config.ssh?.enabled) {
    await manager.connect(config)
    return null
  }
  const tunnel = new SshTunnel()
  try {
    const localPort = await tunnel.open(config.ssh, config.host, config.port)
    await manager.connect({ ...config, host: '127.0.0.1', port: localPort })
    return tunnel
  } catch (err) {
    await tunnel.close() // 接続失敗時にトンネルを残さない
    throw err
  }
}
```

- [ ] **Step 2: ハンドラの差し替え**

- `connections:connect` / `db:connect` の `manager.connect(config)` を `connectWithTunnel(manager, config)` に差し替え、返ったトンネルを main 側のモジュールスコープ（`main/index.ts` で ConnectionManager と並べて保持）に格納。
- `db:disconnect` と「接続一覧へ戻る」処理で `manager.disconnect()` の後に `tunnel?.close()` を呼ぶ。
- SSH 系エラー（auth 失敗・ホスト到達不能・鍵ファイル不存在）は `normalizeDbError` を通すと汎用メッセージになりがちなので、`connectWithTunnel` 内で `code: 'SSH_TUNNEL_FAILED'` の AppError に変換して投げ分けることを検討（normalizeDbError の実装を読んで判断）。

- [ ] **Step 3: typecheck + 全テスト → Commit**

```bash
npm run typecheck && npm test
git add -A && git commit -m "feat: 接続フローに SSH トンネルを組み込み"
```

### Task 8-5: 接続フォームの SSH セクション

**Files:**
- Modify: `src/renderer/src/home/ConnectionFormModal.tsx`
- Modify: `src/preload/index.ts`（鍵ファイル選択ダイアログ用 IPC `file:pickPrivateKey` を追加する場合）
- Modify: `src/main/ipc/registerFileHandlers.ts`（同上: `dialog.showOpenDialog` で鍵ファイルパスを返す）

- [ ] **Step 1: フォーム拡張**

ConnectionFormModal に「SSH トンネルを使う」チェックボックスを追加し、ON のとき以下を表示:

- SSH Host / Port（既定 22）/ User
- 認証方法（パスワード / 秘密鍵）のラジオ
- パスワード入力（authMethod=password）
- 鍵ファイルパス + 「選択…」ボタン（authMethod=privateKey。`~/.ssh/id_ed25519` などを `file:pickPrivateKey` IPC のファイル選択で設定）+ パスフレーズ入力（任意）

既存の「SSH トンネル / SSL は今後対応」の注記（ConnectionFormModal.tsx:130 付近）から SSH の文言を外し、「SSL は今後対応」に変える。

編集時の挙動: パスワード/パスフレーズは renderer に返らないため、既存プロファイル編集時は空欄 = 変更なし（DB パスワードの既存挙動と同じ方式に合わせる）。

- [ ] **Step 2: Test Connection の対応**

フォームの Test Connection（ConnectionFormModal.tsx:48-65 の handleTest）は `db:connect` を呼ぶ構造のはず。Task 8-4 で `db:connect` がトンネル対応済みなので、フォームの ssh 入力値を `ConnectionConfig.ssh` に詰めて送るだけでよい。テスト後は必ず disconnect（既存挙動）+ トンネル close が走ることを確認。

- [ ] **Step 3: 手動 E2E 確認**

確認手順（ローカルで SSH サーバを使う）:

```bash
# macOS のリモートログインを ON にする（システム設定 > 共有）か、
# docker で sshd + mysql を立てて bastion 構成を再現する
# 最小確認: ssh 先 = localhost, DB = 127.0.0.1:3306（テスト用 MySQL）
```

1. フォームで SSH ON / host=localhost / 自分のユーザ / 鍵認証 を入力 → Test Connection 成功
2. 保存 → 接続 → テーブル一覧が出る → クエリ実行できる
3. 切断（接続一覧へ戻る）→ `lsof -iTCP -sTCP:LISTEN | grep node` でトンネルのローカルポートが残っていないこと
4. SSH パスワードを間違えると明確なエラーメッセージが出ること

- [ ] **Step 4: Commit + PR**

```bash
npm run typecheck && npm test
git add -A && git commit -m "feat: 接続フォームに SSH トンネル設定を追加"
git push -u origin feature/ssh-tunnel && gh pr create --title "feat: SSH トンネル接続" --body "..."
```

---

# Phase 9: v0.2.0 リリース作業

Phase 1〜8 がすべて main にマージされてから行う。

### Task 9-1: バージョン更新とリリースノート

**Files:**
- Modify: `package.json`（`"version": "0.2.0"`）
- Modify: `RELEASE_NOTES.md`（v0.2.0 セクションを先頭に追加）

- [ ] **Step 1: バージョンを 0.2.0 に更新**

- [ ] **Step 2: RELEASE_NOTES.md に v0.2.0 セクションを追加**

内容: 本計画の 8 機能を「スキーマ・SQL エディタ強化（構造ビュー / 補完 / 履歴 / EXPLAIN）」「接続強化（SSH トンネル）」「UI 強化（ダークモード / JSON インスペクタ / SQL 結果 CSV）」の 3 カテゴリで記載。v0.1.0 のフォーマットに合わせる。

- [ ] **Step 3: PR → マージ**

```bash
git checkout -b docs/release-0.2.0
git add package.json RELEASE_NOTES.md
git commit -m "docs: v0.2.0 リリースノートとバージョン更新"
git push -u origin docs/release-0.2.0 && gh pr create --title "docs: v0.2.0 リリース準備" --body "..."
```

### Task 9-2: ビルド・配布

- [ ] **Step 1: フル検証**

```bash
npm run typecheck && npm test && npm run dist:mac
```

Expected: `dist/Table++-0.2.0-arm64.dmg` が生成される。**ssh2 がネイティブ依存を含むため、DMG からインストールした実機で SSH 接続を必ず一度確認すること**（v0.1.0 と同じ ad-hoc 署名運用）。

- [ ] **Step 2: GitHub リリース**

v0.1.0 と同じ手順:

```bash
gh release create v0.2.0 --target main --title "v0.2.0" --notes-file RELEASE_NOTES.md \
  "dist/Table++-0.2.0-arm64.dmg#Table++ 0.2.0 (macOS Apple Silicon, unsigned)"
```

注: `--notes-file RELEASE_NOTES.md` は全バージョン分を含むため、v0.2.0 セクションだけを抜き出した一時ファイルを渡すこと。

---

## 完了条件（Definition of Done）

- [ ] 8 機能すべてが main にマージ済み
- [ ] `npm run typecheck` / `npm test` がグリーン
- [ ] ダーク/ライト両方で全画面（接続一覧・グリッド・構造ビュー・履歴・モーダル・コンテキストメニュー）の目視確認済み
- [ ] SSH トンネル経由の接続 → クエリ → 切断 → ポート残留なし、を実機確認済み
- [ ] GitHub release v0.2.0 公開・DMG 添付済み
